const dataTableConfigInit = function() {
    // let api = this.api();

    // api.on('click', 'tbody td', function() {

    //     const columnIndex = api.cell(this).index().column;

    //     if (columnIndex !== 1 || columnIndex !== 5 || columnIndex !== 10 ) {
    //         api.search(this.innerHTML).draw();
    //     }
    // });
};
